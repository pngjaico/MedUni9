conteudos/
node_modules/
.firebase/
.migration-temp/
.superpowers/
$base/
.gemini/
.claude/worktrees/

# Pasta de legado do ciclo clinico (PDFs/docs/slides do aluno + provas antigas).
# Inventario salvo em data/agent_logs/inventario_legado_*.json e versionado em
# git lfs OU mantido apenas local (conforme volume). Conteudo bruto fica fora do repo.
../Dados para expansão ciclo clinico/
../legado_ciclo_clinico/
legado_ciclo_clinico/

# Secrets — NEVER commit
.env
.env.*
.env.local
.agents/firebase-sa-key.json
.agents/firebase-sa-key-OLD.json
.agents/firebase-sa-key-NEW.json
medgradplus-*-adminsdk*.json
*-chave-nova.json
chave privada firebase.json
*chave privada*
*service-account*
*serviceaccount*

# Runtime artifacts from local agents/pipelines
.agents/logs/
data/agent_logs/
data/feedback/archived/*.json
data/feedback/plans/
data/feedback/processed/

# Backups and temporary data
*.bak
data/*_backup_*.json
data/questoes_antigas_backup.json

# Logs
*.log
firebase-debug.log
firestore-debug.log

# Temp/dev scripts
scripts/temp_*
scripts/*.json
scripts/*.ps1

# Dev artifacts in root
*.cjs
publicidade_texto.md
publicidade_visuais.html

# Graphify cache (huge hash-named JSON files, gerados localmente)
graphify-out/cache/
graphify-out/converted/

# Marketing/divulgacao artifacts (nao versionar imagens grandes)
DIVULGAÇÃO ZAP/
DIVULGACAO ZAP/

# Scratch/temp folders
scratch/

# PDF de fontes externas (nao versionar)
Material_Abdome_Agudo_Material_de_Estudo_MedEvo.pdf

# Archive folder (backup local apenas)
_archive/
